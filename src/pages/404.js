import * as React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>NOT FOUND</h1>
      <p>There's no content here, it might have been moved, or you might have a bad link.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
